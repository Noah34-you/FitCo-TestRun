# Optional build-time override for the runtime config in /posthog-config.js.
# If set, the key is baked into the bundle at build time (it is a
# publishable client key, not a secret). Copy to .env to use.
VITE_PUBLIC_POSTHOG_KEY=
VITE_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
